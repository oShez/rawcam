# JDK: use Android Studio's bundled JetBrains Runtime (Java 21) so builds
# work regardless of the system `java` on PATH (which may be Java 8).
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr

org.gradle.jvmargs=-Xmx4g
android.useAndroidX=true
